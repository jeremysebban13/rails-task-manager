<h1>My tasks</h1>
  <ul>
    <% @tasks.each do |task| %>
      <li><%= link_to task.name, task_path(task) %></li>
    <% end %>
  </ul>

  ---------------------------
  <br>
  <%= link_to "add new task", new_task_path %>
</div>

